name: Tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go-tests:
    name: Go tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install ffmpeg
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Run Go tests
        run: go test ./... -tags integration -timeout 30m

  webapp-tests:
    name: Webapp tests
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: webapp
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: webapp/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run unit tests
        run: npm run test

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium firefox

      - name: Run e2e tests
        run: npm run e2e

      - name: Run integration e2e tests
        run: npm run e2e:integration

  ios-unit-tests:
    name: iOS unit tests
    runs-on: macos-26
    timeout-minutes: 45
    env:
      XCODE_VERSION: "26.5"
      IOS_TEST_DESTINATION: platform=iOS Simulator,name=iPhone 17 Pro Max
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s "/Applications/Xcode_${XCODE_VERSION}.app/Contents/Developer"
          xcodebuild -version

      - name: Cache Swift package checkouts
        uses: actions/cache@v4
        with:
          path: |
            iosapp/build/test-derived-data/SourcePackages
            ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-xcode-${{ env.XCODE_VERSION }}-tests-spm-${{ hashFiles('iosapp/iosapp.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved') }}

      - name: Resolve Swift packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project iosapp/iosapp.xcodeproj \
            -scheme iosapp \
            -derivedDataPath iosapp/build/test-derived-data

      - name: Run iOS unit tests
        run: make ios-unit-test

  ios-ui-tests:
    name: iOS UI tests
    runs-on: macos-26
    timeout-minutes: 60
    env:
      XCODE_VERSION: "26.5"
      IOS_TEST_DESTINATION: platform=iOS Simulator,name=iPhone 17 Pro Max
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s "/Applications/Xcode_${XCODE_VERSION}.app/Contents/Developer"
          xcodebuild -version

      - name: Cache Swift package checkouts
        uses: actions/cache@v4
        with:
          path: |
            iosapp/build/uitest-derived-data/SourcePackages
            ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-xcode-${{ env.XCODE_VERSION }}-uitests-spm-${{ hashFiles('iosapp/iosapp.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved') }}

      - name: Resolve Swift packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project iosapp/iosapp.xcodeproj \
            -scheme iosappScreenshots \
            -derivedDataPath iosapp/build/uitest-derived-data

      - name: Run iOS UI tests
        run: make ios-ui-test
