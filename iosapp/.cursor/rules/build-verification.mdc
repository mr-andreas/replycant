- After completing any code changes, always build the project using `xcodebuild -project iosapp.xcodeproj -scheme iosapp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' build` to ensure the app compiles properly.
